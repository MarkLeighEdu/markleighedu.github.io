name: Deploy Jekyll site to Pages

on:
  push:
    branches: ["main"]  # Update to 'main' if your default branch is 'main'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Ruby environment
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'  # Use Ruby 3.1 for better compatibility
          bundler-cache: true  # Automatically installs and caches gems

      - name: Install Bundler
        run: gem install bundler -v 2.2.33  # Install a specific Bundler version

      - name: Install dependencies
        run: |
          bundle clean --force  # Clear Bundler cache
          bundle install --verbose  # Install gems with verbose output

      - name: Build Jekyll site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site  # The default output directory for Jekyll builds

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
